#!/usr/bin/env sh

main() {
    local main="/home/istvan/progs/gamma/src/bin/main.go"
    
    if [ "$1" = "build" ]; then
        go build "${main}"
    else
        go run "${main}" "$*"
    fi
}

main $*