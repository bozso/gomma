#!/usr/bin/env sh

main() {
    local root="/home/istvan/progs/gamma/src/bin"
    local main="${root}/main.go"
    local gamma="${root}/Gamma"
    
    # go build -o "${gamma}" "${main}"
    ${gamma} $*
    
}

main $*