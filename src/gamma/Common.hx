package gamma.common;

typedef CmdFun = Array<String> -> String;

function make_cmd(name: String): CmdFun {
    return function (args: Array<String>): String {
        return "a";
    }
    return inner;
}
